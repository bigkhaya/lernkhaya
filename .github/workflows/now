const myage = 24 ;
var earlyYears = 2;
earlyYears *= 10.5
let laterYears = myage -2 ;
laterYears*=4;
var myhome = `syria`
let dogyeras= earlyYears+laterYears ;
const myName = 'wael'.toLowerCase();
// string concatenation ist grup vorm moor strings معناها انو بس الجمع بين السترينغز 
console.log (`Hallo my name is ${myName} my age is ${myage} im form ${myhome} my age in Dog years is ${dogyeras} `)
// trmplate literlas معنا هاي '
واني بضيف متغيرات على السترينغ
سلسلة enterpolation بمعناها لما اجمع ا لمتغيرات بلسترينجز 


// built in method مثال عنها اني فيني كبر الحروف او صغرها انو هاد الشي بس علي استدعيه مو لازم ابنيهconst myage = 24 ;



const user = "wael";
console.log(` plase einter ur name  `);
let age = (0>18);
switch (age) {
    case (0>18):
        age <=17 ? console.log(`sorry ${user} u r -18`) : console.log(`u r wellcomm ${user}` );
}

const user = "wael";
console.log(` plase einter ur name  `);
let age = (0>18);
switch (age) {
    case (0>18):
        age <=17 ? console.log(`sorry ${user} u r -18`) : console.log(`u r wellcomm ${user}` );
}
